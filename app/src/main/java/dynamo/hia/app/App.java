/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package dynamo.hia.app;

import dynamo.hia.list.LinkedList;

import static dynamo.hia.utilities.StringUtils.join;
import static dynamo.hia.utilities.StringUtils.split;
import static dynamo.hia.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
