package nl.rivm.emi.dynamo.data.types.interfaces;

/**
 * This interface defines the function in the model Object.<br/>
 * The ContainerType interface is implemented by values that index Map-levels in
 * the modelobject.<br/>
 * A type implements either a PayloadType, a ContainerType, a WrapperType or a RootElementType.
 * 
 * @author mondeelr
 * 
 * @param <T>
 *            The Type of the value in the modelobject.
 */
public interface ContainerType<T> {

}
