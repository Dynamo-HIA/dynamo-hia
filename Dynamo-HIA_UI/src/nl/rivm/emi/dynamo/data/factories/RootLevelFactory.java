package nl.rivm.emi.dynamo.data.factories;

/**
 * Marker interface to provide a common ground between the AgnosticFactory (that
 * processes hierarchical configuration files with on or more payload fields)
 * and the AgnosticGroupFactory (that processes configuration files that have
 * diffenrent rootchild-element types).
 * 
 * @author mondeelr
 * 
 */
public interface RootLevelFactory {

}
